
export interface AppointmentInput {
    date: string;
    time: string;
    doctorId: string;
    petId: string;
    ownerId: string;
}

export interface AppointmentUpdateInput {
    date: string;
    time: string;
    doctorId: string;
    petId: string;
    ownerId: string;
}